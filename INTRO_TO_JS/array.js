// array: 

//which can store one or more values at any given time and it treats array's as obejects and it is represented
//by [  ]
//use the join() property also


let sports = ['football, soccer, GOLF, hockey, RugBY, Basketball']
console.log(`\nsports --> ${sports}`);
//sports is a variable which contains an array of strings 

let digits = [10, 45, 32, 789]

//digits is a variable which contains an array of numbers 
let bills = [100.00, 55.87, 236]
console.log(`\nsportsLength --> ${sportsLength}`);
//name is array of numbers

//to find the length of an array use the property lenth

//length property 
//multiple objects stored and always within double brakets []
//sports is a variable which contains or stores array of strings

//--> values in an array are getting stored in the concept of index 
//*****first value of the array gets stored at index 0 and so on INDEX RULES 
//Use of square bracket with an array - 1. To get the value at any given index

let valueAt2= sports[2];

console.log \nThe value at index-2 in sports-array  --> ${}
//To assign the value at a particular index in the array
console.log('\n2. To assign the value at a particular index in the array');

sports[1] = "ruNNing";
console.log(`\nsports --> ${sports}`);

sports[4] = sports[4].toUpperCase();

1. To get the value present at any given index
.. To assign the value at a porticuter index in the array
console. top/"LaUse of 11 with an Array')e
1 1. To get the value present at any given index
console. lag/*Val. Te get the value present at any given index*)s
let valueAtz
sports tal:
I/ gives the value/data present at inden-7 do sports-array
console, lag(" In The value at indax-2 In sports-Array - S(vatuext2)* )g
console. lag(* sports (3) • toLoverCase() - $(sports (3]. totavarCase())))
console, lael*sports(3). length - ${sports (3] Length)' )g
// 2. To assign the value at a particular index in the array a
console. tap/*\a2, To assign the value at a particular Index in
the array')
sports (1) • * ruling*;
// storing
the value (ruiNiNg) at
Index-1 in sports-Array
console. lag/* Insports - $(sports)');
sportst4) - sportsis)-t0upperCasel):
(/ change index-4 into uppercase
sports (4). toupperCase?)
it and converting into Uppercase
getting the
value from index-4 and copying
result after solving righthand side of a sign, result
sports (4) =
whatever is the
(/ only print the uppercase-result sult
sports (4). Compan

/**
 * Print true/false
 * if the last-value in the array has length more than 10
 *      print true
 * otherwise
 *      print false
 * 
 * 
 * sports = ['FootBall', 'SoCCer', 'GOLf', 'HocKEy', 'MaraTHon', 'RuGBy', 'BasketBaLl']
 * 
 * 
 * sports = ['football, soccer, GOLF, hockey, RugBY, Basketball']

*/

sports = ['football, soccer, GOLF, hockey, RugBY, Basketball'];
let sportsLastIndex = sports.length - 1;
let lastValueLenghtGr10 = last

/**
 * Convert the given word into Titlecase
 * 
 * lAUgh        ->      Laugh
 * heLLo        ->      Hello
 * grEetIngs    ->      Greetings
 */

function convertToTitleCase(word) {
    // Convert the word to lowercase
    var lowercaseWord = word.toLowerCase();
  
    // Capitalize the first letter and concatenate it with the rest of the word
    var titleCaseWord = lowercaseWord.charAt(0).toUpperCase() + lowercaseWord.slice(1);
  
    return titleCaseWord;
  }
  
  // Example usage
  var word = 'lAUgh';
  var titleCaseWord = convertToTitleCase(word);
  console.log(titleCaseWord);
  

  //convert all to lowercase
  //2. get first letter
  //convert first word to uppercase 
  //
  //1. convert cord into lowercase
  //2. use split() into individual letters
  //3. convert index-0-array into uppercase
  //4. join all values of the array 

  
NELLO
nette
grEetings
Greetings
console. lag(* InConvert the given sore into Tittecase);
Let giventord - 'grEatings')
* LAugh*
convert ward Into lovercase
platt) inte individual letters
3. convert index-4-array into uppercase
join alt values of array
consoles lag/*giventord (before conversion) - Statvenvord)*):
18283:3:230850048898985888835
let givenlord_LoverCase:
// Jain all values of array
Let results - givenkord_Sput.jein(*)1
Laugh
console, tagl' giventard (after conversion) -n $(resutil))
* LAugh*
convert ward into Lowercase
first-letter (use charat)
vert first-letter Into uppercase
*/
//if user does not input the separtor 
//pop always removes the last value 

//function shift removes the first value index0

//To remove value of an array = pop shift 

//To add values called push 
//Push adds the new value to the end of array 
//input: value to add 
//return: the new value to add 

//unshift - adds a new value at the end of the array 
//whenever adding a value you will print the new length 

sports = [GOLF, HocKey, MarathOn];
digits = [45, 76, 87, 35, 23];

console.log('\nfunction - push(');
console.log(`sports --> ${sports}`);
console.log(`digits --> ${digits}`);

let sportsNewLength1 = spots.push('badmitton');
console.log(`new length after push()`) = 

console.log('\njoin(');
console.log(`sports --> ${sports}`);
let sportsJoin = sports.join();



