//Double Operator 
//Triple Operator 
//the == operator performs type conversion whereas the === does Notification, 
//triple operator is faster 

//strict equal 
let productPrice = 10;
let amountInWallet = 30;
//strict equal ===
console.log(productprice === amountInWallet)

//strict not equal !==
console.log(productPrice !== amountInWallet);

//Greater than >
console.log(amountInWallet > productPrice);
//lessthan <
console.log(amontInWallet < ProductPrice);

//greater than or equal to >=
console.log(amountInWallet >= productPrice);

//less than or equal to <=
console.log(amountInWallet <= productPrice);
//use triple === when comparing two values 
